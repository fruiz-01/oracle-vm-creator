name: OCI ARM VM - Auto Retry

on:
  workflow_dispatch:
    inputs:
      max_attempts:
        description: 'Número máximo de intentos (0 = infinito)'
        required: false
        default: '0'
      wait_seconds:
        description: 'Segundos entre intentos'
        required: false
        default: '30'
  schedule:
    - cron: '0 * * * *'

concurrency:
  group: oci-arm-vm
  cancel-in-progress: true

jobs:
  create-vm:
    runs-on: ubuntu-latest
    timeout-minutes: 350

    steps:
      - name: Setup OCI CLI
        run: |
          pip install oci-cli --quiet

      - name: Configure OCI credentials
        run: |
          mkdir -p ~/.oci
          echo "${{ secrets.OCI_PRIVATE_KEY }}" > ~/.oci/private_key.pem
          chmod 600 ~/.oci/private_key.pem
          cat > ~/.oci/config << EOF
          [DEFAULT]
          user=${{ secrets.OCI_USER_OCID }}
          fingerprint=${{ secrets.OCI_FINGERPRINT }}
          tenancy=${{ secrets.OCI_TENANCY_OCID }}
          region=${{ secrets.OCI_REGION }}
          key_file=~/.oci/private_key.pem
          EOF
          chmod 600 ~/.oci/config

      - name: Verify OCI connection
        run: |
          oci iam region list --output table

      - name: Try to create ARM VM (with retries)
        env:
          IMAGE_ID: ${{ secrets.OCI_IMAGE_ID }}
          SUBNET_ID: ${{ secrets.OCI_SUBNET_ID }}
          SSH_KEY: ${{ secrets.OCI_SSH_KEY }}
          MAX_ATTEMPTS: ${{ github.event.inputs.max_attempts || '0' }}
          WAIT_SECONDS: ${{ github.event.inputs.wait_seconds || '30' }}
        run: |
          attempt=0

          AD=$(oci iam availability-domain list \
            --compartment-id ${{ secrets.OCI_TENANCY_OCID }} \
            --query 'data[0].name' \
            --raw-output)

          echo "Availability Domain: $AD"
          echo "Imagen: $IMAGE_ID"
          echo "Subnet: $SUBNET_ID"
          echo ""

          while true; do
            attempt=$((attempt + 1))
            echo "=========================================="
            echo "🔄 Intento #$attempt - $(date '+%Y-%m-%d %H:%M:%S')"
            echo "=========================================="

            RESULT=$(oci compute instance launch \
              --availability-domain "$AD" \
              --compartment-id "${{ secrets.OCI_TENANCY_OCID }}" \
              --shape "VM.Standard.A1.Flex" \
              --shape-config '{"ocpus": 4, "memoryInGBs": 24}' \
              --image-id "$IMAGE_ID" \
              --subnet-id "$SUBNET_ID" \
              --assign-public-ip true \
              --display-name "arm-free-tier" \
              --ssh-authorized-keys-file <(echo "$SSH_KEY") \
              --metadata '{"user_data": ""}' \
              2>&1) && SUCCESS=true || SUCCESS=false

            if [ "$SUCCESS" = "true" ]; then
              echo ""
              echo "✅ ¡VM CREADA EXITOSAMENTE!"
              echo "$RESULT" | python3 -c "
          import sys, json
          data = json.load(sys.stdin)
          inst = data['data']
          print(f\"  ID:     {inst['id']}\")
          print(f\"  Name:   {inst['display-name']}\")
          print(f\"  State:  {inst['lifecycle-state']}\")
          print(f\"  Shape:  {inst['shape']}\")
          print(f\"  OCPUs:  {inst['shape-config']['ocpus']}\")
          print(f\"  RAM:    {inst['shape-config']['memory-in-gbs']} GB\")
          "
              echo "🎉 La VM ARM gratuita fue creada. Workflow finalizado."
              exit 0
            else
              if echo "$RESULT" | grep -qi "Out of host capacity\|capacity\|InternalError\|NotAuthorizedOrNotFound"; then
                echo "⏳ Sin capacidad disponible (normal). Esperando $WAIT_SECONDS segundos..."
              else
                echo "⚠️ Error inesperado:"
                echo "$RESULT"
                echo "Esperando $WAIT_SECONDS segundos antes de reintentar..."
              fi

              if [ "$MAX_ATTEMPTS" -gt "0" ] && [ "$attempt" -ge "$MAX_ATTEMPTS" ]; then
                echo "❌ Se alcanzó el máximo de $MAX_ATTEMPTS intentos sin éxito."
                exit 1
              fi

              sleep $WAIT_SECONDS
            fi
          done
